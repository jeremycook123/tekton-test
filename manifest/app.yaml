apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodejs-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nodejs-app
  template:
    metadata:
      labels: 
        app: nodejs-app
    spec:
      containers: 
      - name: nodejs-app 
        image: cloudacademydevops/nodejs-app:latest
        ports:
        - containerPort: 8081
---
apiVersion: v1
kind: Service
metadata:
  name: nodejs-service
  labels:
    env: demo
spec:
  ports:
   - protocol: TCP
     port: 80
     targetPort: 8081
  selector:
    app: nodejs-app
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: nodejs-service
  labels:
    env: demo
spec:
  to:
    kind: Service
    name: nodejs-service
  port:
    targetPort: 80
